spring:
  docker:
    compose:
      enabled: false

app:
  cep:
    base-url: ${CEP_API_BASE_URL:http://localhost:8082}
    connect-timeout: 1s
    read-timeout: 1s

management:
  server:
    port: 0

logging:
  level:
    br.com.leonardomachado.petshop: DEBUG
